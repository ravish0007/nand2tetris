CHIP FullAdder {
    IN a, b, c;  // 1-bit inputs
    OUT sum,     // Right bit of a + b + c
        carry;   // Left bit of a + b + c

    PARTS:
    //// Replace this comment with your code.
    HalfAdder(a=a, b=b, sum=aplusb , carry=abcarry );
    HalfAdder(a=aplusb, b=c , sum=sum , carry=temp );
    Or(a=temp , b=abcarry, out=carry);


}
